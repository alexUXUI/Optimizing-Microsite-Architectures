import React from "react";

const style = {
  background: "tomato",
  color: "#fff",
  padding: 12,
  border: "none",
  borderRadius: "4px",
  fontSize: "20px",
};

const Button = () => <button style={style}>App 1 Button</button>;

export default Button;
