import React from "react";

const style = {
  background: "#19b868",
  color: "#fff",
  padding: 12,
  border: "none",
  borderRadius: "4px",
  fontSize: "20px",
};

const Button = () => <button style={style}>App 2 Button</button>;

export default Button;
